<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Jika file/folder aslinya ada, langsung buka saja
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Jika tidak ada, lempar ke index.php
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
